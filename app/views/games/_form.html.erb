<%= form_for @game do |f| %> 

<%= f.select :category, options_for_select([['Currently Playing', "current"], ['Want To Play', "to_play"],  ['Finished', "finished"]], selected: @game.category || nil),
         include_blank: false, class:'form-control m-b'%>

<%= f.submit %>

<% end %>